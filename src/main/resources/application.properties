# Application
spring.application.name=utility-billing-system
server.port=8080

# PostgreSQL (default database)
spring.datasource.url=jdbc:postgresql://localhost:5432/utility_billing
spring.datasource.username=postgres
spring.datasource.password=Nad1ne11
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# JWT
app.jwt.secret=WasacRegUtilityBillingSystemSecretKey2024MustBeAtLeast256BitsLongForHS256Algorithm
app.jwt.expiration-ms=86400000

# Swagger / OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.filter=true
springdoc.swagger-ui.display-request-duration=true
springdoc.swagger-ui.doc-expansion=list
springdoc.show-actuator=false
springdoc.packages-to-scan=com.wasac.billing.controller

# Install DB triggers on startup (bill/payment notifications)
app.db.triggers.enabled=true

# OTP email verification
app.otp.expiration-minutes=10
app.otp.length=6
app.mail.enabled=true

# Gmail SMTP — use a Google App Password (not your normal Gmail password)
# Create at: https://myaccount.google.com/apppasswords
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nadineizabayo06@gmail.com
spring.mail.password=wldc eggq mzfm skhe
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
